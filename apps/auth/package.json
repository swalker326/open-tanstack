{
  "name": "@repo/auth",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --persist-to ../../.wrangler/state --port 8788 --inspector-port 9231",
    "start": "wrangler dev --persist-to ../../.wrangler/state --inspector-port 9231",
    "test": "vitest",
    "cf-typegen": "wrangler types",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "@cloudflare/workers-types": "^4.20250427.0",
    "@repo/database": "workspace:*",
    "@repo/subjects": "workspace:*",
    "typescript": "^5.5.2",
    "vitest": "~3.0.7",
    "wrangler": "^4.70.0"
  },
  "dependencies": {
    "@openauthjs/openauth": "^0.4.3",
    "zod": "^4.1.11"
  }
}
